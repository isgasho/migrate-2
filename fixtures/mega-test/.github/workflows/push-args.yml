on: push
name: args
jobs:
  stringArgs:
    name: string args
    runs-on: ubuntu-latest
    steps:
    - name: string args
      uses: docker://alpine
      with:
        args: echo hi
    - name: array args
      uses: docker://alpine
      with:
        args: echo hi
