on: push
name: workflow one
jobs:
  actionOne:
    name: action one
    runs-on: ubuntu-latest
    steps:
    - name: action one
      uses: docker://alpine
      with:
        entrypoint: sh -c echo ${{ github.sha }}
