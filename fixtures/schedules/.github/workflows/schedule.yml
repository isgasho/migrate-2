on:
  schedules:
  - cron: '* * * * *'
name: per minute
jobs:
  actionOne:
    name: action one
    runs-on: ubuntu-latest
    steps:
    - name: action one
      uses: docker://alpine
      with:
        entrypoint: sh
        args: -c "echo ${{ github.sha }}"
